package ballboy.entity;

public class Tree extends EntityParent {
    public Tree(double xPos, double yPos, double height, double width, String imagePath, Layer layer) {
        super(xPos, yPos, height, width, imagePath, layer);
    }
}
